import { buildMetadata } from '@/lib/seo';
import { PageShell } from '@/components/cliente/ClientAreaLayout';
export const metadata = buildMetadata({ title:'Monitoramento da Usina Solar | SolarInvest', description:'Aprenda a acompanhar geração diária, mensal, histórico, alertas e indicadores saudáveis, de atenção e críticos.', path:'/area-do-cliente/monitoramento', keywords:['monitoramento solar','manutenção de energia solar'] });
export default function Page(){const cols=[['Indicadores saudáveis','Geração coerente com clima, histórico estável, inversor online e sem alarmes.'],['Indicadores de atenção','Queda gradual, alarmes intermitentes, sujeira visível ou diferença entre strings.'],['Indicadores críticos','Inversor parado, geração zerada em dia útil, cabo exposto, cheiro de queimado ou falha recorrente.']]; return <PageShell title="Monitoramento da Usina" description="Acompanhe geração diária, geração mensal, histórico e alertas para agir antes que pequenas perdas virem grandes prejuízos."><div className="mt-10 grid gap-6 md:grid-cols-3">{cols.map(([h,p])=><div key={h} className="rounded-3xl bg-white p-8 shadow-sm"><div className="mb-5 h-24 rounded-2xl bg-gradient-to-r from-orange-100 to-amber-100" /><h2 className="text-xl font-black">{h}</h2><p className="mt-3 text-slate-700">{p}</p></div>)}</div></PageShell>}
